# Dotfiles

## Setup
```
# Clone this repo to root directory and recursively install all submodules
cd;git clone --recursive https://github.com/carlbaron/dotfiles.git

# Install dotfiles
chmod +x ~/dotfiles/install.sh
~/dotfiles/install.sh
```
